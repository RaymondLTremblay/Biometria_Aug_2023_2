# Estadística Descriptiva



```{r c08-1, eval=TRUE, echo=FALSE}
colorize <- function(x, color) {
  if (knitr::is_latex_output()) {
    sprintf("\\textcolor{%s}{%s}", color, x)
  } else if (knitr::is_html_output()) {
    sprintf("<span style='color: %s;'>%s</span>", color, 
      x)
  } else x
}

#`r colorize("some words in red", "red")`


```

Fecha de la última revisión
```{r c08-2, echo=FALSE}

Sys.Date()
```


```{r c08-3, echo=FALSE, fig.show = "hold", out.width = "20%", fig.align = "default"}
knitr::include_graphics(c("Graficos/hex_ggversa.png", "Graficos/hex_error.png"))
```



Activar los Paquetes
```{r c08-4, warning=FALSE}

library(ggplot2)  # paquete para visualizar los datos
library(ggversa) # paquete para diferentes conjuntos de datos
library(modeest) # paquete para calcular la moda 

library(pastecs) # paquete para análisis tiempo-espacial usado en ecología 
library(psych) # paquete para análisis psicométrica, psicológica y de personalidad 
library(knitr) # un grupo de función para incluyendo tablas bonitas con kable.
library(tidyverse)
library(gridExtra)
library(e1071)

```

***
## Estadística descriptiva

En los módulos de "Medidas de tendencia central" y "Medidas de dispersión" se explicó de dónde provienen estos parámetros y cómo calcularlos. En este módulo aprenderemos distintas funciones para calcularlos individualmente, y herramientas para obtenerlos todos (y otros) a la vez.  


Los índices que veremos aquí incluyen

  - el promedio
  - la mediana
  - la desviación estándar
  - el mínimo
  - el máximo
  - los cuantiles
  - el índice de oblicuidad (en inglés "Skewness")
  - el índice de curtosis (en inglés "Kurtosis")

Aquí creamos un conjunto de 100 datos con un promedio de 100 y una desviación estándar de 10. Nota que la función **set.seed()** hace que la simulación comience igual cada vez que se corre, dando el mismo resultado. Esto se usa solo cuando uno está enseñando, para que los resultados sean consistentes.   
 
 
```{r c08-5, warning=FALSE}

# rnorm(n=100, mean=100, sd=10) # genera un número al azar de una distribución normal


set.seed(12345) # para que los resultados sean consistentes
x=rnorm(100, 100, 10) #(100 datos, x=100, sd=10)

#x 
df=data.frame(x)
#df

mean(df$x) # el promedio
sd(df$x) # la desviación estándar
min(df$x) # el valor mínimo
max(df$x) # el valor máximo
```

***


  1. Quiz 1:
    Usa R y construye una lista o data frame con los siguientes datos y calcular el promedio.  Contesta en **MSTeam Form** con tu respuesta.  4, 6, 7, 3, 9, 10, 19, 52. 
    
***


## Análisis con muchos datos

Usaremos datos que hemos visto en el módulo Producción de Gráficos.  

Se necesita el archivo **DownloadFestival** que se encuentra debajo la pestaña de **Los Datos**. El ejemplo proviene de Field et al. (2014).


>  Una bióloga estaba preocupado por los posibles efectos sobre la salud de los que particpan a un festivales de música. Entonces, un año fue al Download Festival en el Reino Unido (Download Festival UK). Ella midió la higiene del los que participaron al concierto n= 810 durante el festival de 3 días. Cada día intentaba encontrar a todas las personas que censó el primer día. Los valores asignado fueron de 0 a 4 sobre el nivel de limpieza por como olia los participantes

>    + 0 = hueles como un cadáver. 
>    + 4 = hueles a rosas dulces en un fresco día de primavera
  
  
  
  
```{r c08-6}
library(readr)
Karn_Penrose_infant_survivorship <- read_csv("Data_files_csv/Karn_Penrose_infant_survivorship.csv")

KP=Karn_Penrose_infant_survivorship  #usamos un nombre más corta para facilitar

head(KP)


```
  
  
```{r c08-7}
summary(KP)
```
  
```{r c08-8}
library(ggplot2)

ggplot(KP, aes(Weigth_lb))+
  geom_histogram(color="white", fill="orange")
```
  
  
  
  >  Una bióloga estaba preocupado por los posibles efectos sobre la salud de los que particpan a un festivales de música. Entonces, un año fue al un Festival en el Reino Unido (Download Festival UK). Ella midió la higiene del los que participaron al concierto n= 810 durante el festival de 3 días. Cada día intentaba encontrar a todas las personas que censó el primer día. Los valores asignado fueron de 0 a 4 sobre el nivel de limpieza por como olia los participantes

>    + 0 = hueles como un cadáver. 
>    + 4 = hueles a rosas dulces en un fresco día de primavera 


```{r c08-9, warning=FALSE}

library(readr)
DownloadFestival_No_Outlier <- read_csv("DownloadFestival_No_Outlier.csv")

dlf=DownloadFestival_No_Outlier  #usamos un nombre más corta para facilitar  
head(dlf) # ver las 6 primeras filas
dlf
```

## Remover los **NA** del análisis

En los datos del festival, en algunos días hay participantes sin datos, así que se añadió un **NA**. En esos casos, para que el análisis funcione, hay que añadir el argumento **na.rm=TRUE**, que significa remover los **NA**. Para darte cuenta, quita **na.rm=TRUE** cuando uses "day2" o "day3" y evalúa qué pasa.  
 
```{r c08-10, warning=FALSE}

library(tidyverse)


mean(dlf$day2, na.rm=TRUE) # na.rm= na remove
sd(dlf$day1, na.rm=TRUE)
min(dlf$day1, na.rm=TRUE)
max(dlf$day1, na.rm=TRUE)
median(dlf$day1, na.rm=TRUE)


```

***

***

## Resumen estadístico de una variable

Para ver los estadísticos mencionados arriba (menos la moda, la oblicuidad y la curtosis) se puede usar la función **stat.desc()** del paquete **pastecs**. Para facilitar la lectura se usa la función `round(x, 3)`; el 3 indica la cantidad de decimales que se muestran. Si no usamos **round()**, los valores aparecen en notación científica.  

Nota que hay muchos más parámetros calculados.  Aparece en la lista en orden el valor mínimo: min(), el valor máximo: max(), la mediana: median, el promedio: mean, la desviación estándar; std.dev entre otros.  

```{r c08-11, warning=FALSE}

library(pastecs)
stat.desc(dlf[,c("day1")])

round(stat.desc(dlf[,c("day1")]), 2)  
```

***

## Resumen Estadístico de multiples variables

Si uno quiere evaluar múltiples variables continuas a la vez, se puede usar la misma función; dentro de **c()** se añaden todas las variables de interés.   

```{r c08-12, warning=FALSE}
mean(dlf$day1, na.rm=TRUE)
round(stat.desc(dlf[,c("day1","day2","day3")]), digits=3) # round reduce a 3 valores significativo
```

***

3. Quiz 3:
    Usa R usa el data.frame **Camas_Hospital** en el paquete **ggversa**.  Contesta en **MSTeam Form** con tu respuesta.  Selecciona la variable **Camas**, que representa el número de camas por cada 1000 personas en muchos países. ¿Cuál es el rango (range)?  


 
***
 
## Los Cuantiles
 
 Los cuantiles son los valores en intervalos específicos de una variable aleatoria continua. A menudo dan una mejor interpretación de la distribución cuando los valores no siguen una distribución normal. Típicamente, la distribución se divide en 4 partes (los cuantiles 0.25, 0.50 = la mediana, 0.75), que se conocen como **cuartiles**. Para más detalle pueden ver el siguiente enlace <https://en.wikipedia.org/wiki/Quantile>. 
 
 En el siguiente ejemplo se muestra cómo utilizar la función **quantile** y seleccionar los cuantiles deseados con el componente **probs = c(...)**.  
 
```{r c08-13, warning=FALSE}

quantile(dlf$day1,probs=c(0.05, 0.1, 0.25, 0.5, 0.75, 0.95, 0.99), na.rm=TRUE)
```


Se puede usar también la función **describe** del paquete **psych**, que da automáticamente estos cuantiles.  


```{r c08-14, warning=FALSE}
describe(dlf$day1)
```


***

## El índice de oblicuidad (Skewness)

::: {.definicion}
La **oblicuidad** (*skewness*) mide la **asimetría** de una distribución. Es **positiva** cuando la cola larga está a la derecha, **negativa** cuando está a la izquierda, y ≈ 0 cuando la distribución es simétrica.
:::

El índice de oblicuidad describe la (a)simetría de una distribución alrededor de su promedio. Otra forma de describirlo es como el **tercer momento**, porque las diferencias respecto al promedio se elevan a la potencia 3. 

La fórmula es la siguiente. Es muy similar a la de la varianza, pero note que las diferencias se elevan a la potencia 3. Para más información vea este enlace <https://en.wikipedia.org/wiki/Skewness>.     

$$\text{Oblicuidad} = \frac{1}{N}\sum_{i=1}^N\left(\frac{x_i-\overline{x}}{\sigma}\right)^3$$

Primero voy a crear tres conjuntos de datos

+ con distribución normal
+ sesgada a la derecha (oblicuidad positiva)
+ sesgada a la izquierda (oblicuidad negativa)

```{r c08-15, warning=FALSE}
normal=rnorm(100000, .5, .15)
obl_derecha=rbeta(100000, 1.5,5)
obl_izquierda=rbeta(100000, 5.5,2)

normal=as_tibble(normal)

obl_derecha=as_tibble(obl_derecha)

obl_izquierda=as_tibble(obl_izquierda)

```


Ahora unimos los data frames y se añade nombres a las columnas

```{r c08-16, warning=FALSE}
df=cbind(normal, obl_derecha, obl_izquierda)
head(df, n=2)
df <- setNames(df, c("normal","obl_derecha","obl_izquierda"))

head(df, n=2)

```
El próximo paso es apilar cada columna una sobre la otra.  La razón que queremos esto es que deseamos reproducir las variables en un mismo gráfico 


```{r c08-17, warning=FALSE}
library(tidyverse)
df2=df%>%
  gather(key = "Distribución", value="valor", c(normal, obl_derecha, obl_izquierda))
head(df2, n=3)
# unique(df2$Distribución) # función para ver el nombre de las variables en la columna Distribución
```

Ahora vamos a ver los datos un gráfico. Vemos que la distribución de los datos son muy diferentes, tanto la distribución en azul y verde están sesgado a unos valores y tienen una cola o valores más grande (azul) o pequeños (verde) que si fuese una distribución normal.   


```{r c08-18, warning=FALSE}
ggplot(df2, (aes(valor, colour=Distribución)))+
  geom_density()+
  xlim(-.01,1)

ggsave("Graficos/Skewness.png")
```


Ahora se calcula el índice de oblicuidad y comparamos los valores. Como regla, cuando el nivel de oblicuidad está entre

+ -0.5 y 0.5 se considera dentro de una distribución simétrica (normal).
+ -1.0 y -0.5, o entre 0.5 y 1.0, los valores tienen una oblicuidad moderada.
+ Menor de -1.0 o mayor de 1.0 los datos tienen una oblicuidad grande.  


Ahora evaluamos la oblicuidad de las tres distribuciones usando la función **skewness** del paquete **e1071**.

Para una distribución normal (simétrica) el valor es muy cercano a cero. Los datos **sesgados a la derecha** (con cola hacia la derecha) tienen oblicuidad **positiva**, y los **sesgados a la izquierda**, oblicuidad **negativa**.  

```{r c08-19, warning=FALSE}
library(e1071)
e1071::skewness(normal$value) # la oblicuidad de los datos de una distribución normal
e1071::skewness(obl_derecha$value) # sesgada a la derecha -> oblicuidad positiva
e1071::skewness(obl_izquierda$value) # sesgada a la izquierda -> oblicuidad negativa
```




***

## El índice de curtosis (Kurtosis)

::: {.definicion}
La **curtosis** (*kurtosis*) mide el peso de las **colas** de una distribución. En exceso, es **positiva** cuando las colas son pesadas (más valores atípicos que la normal) y **negativa** cuando son ligeras. La normal tiene curtosis en exceso ≈ 0.
:::

El índice de curtosis describe las **colas** de una distribución. Otra forma de describirlo es como el **cuarto momento**, porque las diferencias respecto al promedio se elevan a la potencia 4. La curtosis mide la propensión a tener datos atípicos (colas pesadas).  

La fórmula es la siguiente. Es muy similar a la de la varianza, pero note que las diferencias se elevan a la potencia 4. Para más información vea este enlace <https://www.wikiwand.com/en/Kurtosis>.     

$$\text{Curtosis} = \frac{1}{N}\sum_{i=1}^N\left(\frac{x_i-\overline{x}}{\sigma}\right)^4$$

Primero voy a crear cuatro conjuntos de datos

+ con distribución normal
+ con distribución normal, con cola reducida
+ con distribución normal, con cola extendida
+ distribución uniforme
 

```{r c08-20, warning=FALSE}
library(PearsonDS)
library(rmutil)

momentsR=c(mean=0, variance=1, skewness=0, kurtosis=2)
momentsE=c(mean=0, variance=1, skewness=0, kurtosis=4)  

normalR=rpearson(100000, moments=momentsR)
normalE=rpearson(100000, moments=momentsE)


Unif=runif(100000, -2,2)
normal=rnorm(100000, 0,1)
laplace=rlaplace(500000, m=0, s=1)


normal=as_tibble(normal)

normalR=as_tibble(normalR)

normalE=as_tibble(normalE)

Unif=as_tibble(Unif)

laplace=as_tibble(laplace)

```


Ahora unimos los data frames y se añade nombres a las columnas

```{r c08-21, warning=FALSE}
df=cbind(normal, normalR, normalE, Unif, laplace)
head(df, n=2)
df <- setNames(df, c("normal","normalR","normalE", "Unif", "laplace"))

head(df, n=2)

```
El próximo paso es apilar cada columna una sobre la otra.  La razón que queremos esto es que deseamos reproducir las variables en un mismo gráfico 


```{r c08-22, warning=FALSE}
library(tidyverse)
df2=df%>%
  gather(key = "Distribución", value="valor", c(normal, normalR, normalE, Unif, laplace))
head(df2, n=3)
# unique(df2$Distribución) #función  para ver el nombre de las variables en la columna Distribución
```

Ahora vamos a ver los datos en un gráfico. Vemos que la distribución de los datos son muy diferentes, Hay que concentrarse no en el pico de la distribución, sino en las colas de los datos. Fíjate en la distribución normal (color amarillo) y compara si las colas de las demás están por debajo o por encima de ella.  

Tanto la distribución uniforme (color rosa) como la normal reducida (normalR, color azul) pasan por debajo de la curva normal. Al contrario, las líneas verde y roja pasan por encima, por lo que sus colas son más predominantes. 


```{r c08-23, warning=FALSE}
whole=ggplot(df2, (aes(valor, colour=Distribución)))+
  geom_density(adjust=5)+
  xlim(-5,5)

ggsave("Graficos/curtosis_whole.png")


sub=ggplot(df2, (aes(valor, colour=Distribución)))+
  geom_density()+
   theme_bw() +
  scale_x_continuous(limits=c(-5, 1)) + 
  scale_y_continuous(limits=c(0, .1)) +
  theme(legend.position= "none")

ggsave("Graficos/curtosis.png")

sub + annotation_custom(
    ggplotGrob(whole),
    xmin = -1.8, xmax = 1.4, ymin = 0.005, ymax = 0.075)


```


Ahora se calcula el índice de curtosis (en exceso) y comparamos los valores. Cuando se acerca a cero no hay exceso de curtosis, que es lo que uno espera de una distribución normal:

+ Menor de -1.0 los datos están muy aplanados (Uniforme, normalR).
+ Mayor de 1.0 (la distribución de Laplace).  


Ahora evaluamos la curtosis de las distribuciones usando la función **kurtosis** del paquete **e1071**.

Para una distribución normal el valor es muy cercano a cero. Cuando hay **exceso de colas** (colas pesadas, como en Laplace) la curtosis es **positiva**; cuando las colas son ligeras (como en la uniforme) la curtosis es **negativa**.

```{r c08-24, warning=FALSE}
library(e1071)
e1071::kurtosis(normal$value) # curtosis de los datos de una distribución normal
e1071::kurtosis(normalR$value) # curtosis de los datos restringido
e1071::kurtosis(normalE$value) # curtosis de los datos con más colas
e1071::kurtosis(Unif$value) # curtosis de una distribución uniforme, colas ligeras
e1071::kurtosis(laplace$value) # curtosis de distribución Laplace, exceso de colas

```

Quiz 1

```{r c08-25, echo=FALSE, eval=FALSE}
x=c(4, 6, 7, 3, 9, 10, 19, 52)
mean(x)
```

Quiz 2

```{r c08-26, echo=FALSE, eval=FALSE}
y=c(1, NA, 1, 2, 73, 6, 2, 78, 16, 16, 87, 5)
median(y, na.rm=TRUE)
```

Quiz 3

```{r c08-27, echo=FALSE, eval=FALSE}
library(ggversa)
head(Camas_Hospital)

round(stat.desc(Camas_Hospital[,c("Camas")]), 3)

library(e1071)

skewness(Camas_Hospital$Camas)
kurtosis((Camas_Hospital$Camas))
```

```{r c08-28, echo=FALSE, eval=FALSE}
ggplot(Camas_Hospital, aes(Camas))+
  geom_histogram()
```




```{r c08-29, echo=FALSE, eval=FALSE}
dfNormal=data.frame(data=rnorm(1000000))
head(dfNormal)

ggplot(dfNormal, aes(data))+
  geom_histogram(colour="white", fill="orange")+
  xlim(-4, 4)

ggsave("Normal.png")
```

