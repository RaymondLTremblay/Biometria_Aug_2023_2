# Regresión Lineal



```{r c18-1, eval=TRUE, echo=FALSE}
colorize <- function(x, color) {
  if (knitr::is_latex_output()) {
    sprintf("\\textcolor{%s}{%s}", color, x)
  } else if (knitr::is_html_output()) {
    sprintf("<span style='color: %s;'>%s</span>", color, 
      x)
  } else x
}

#`r colorize("some words in red", "red")`


```

Fecha de la última revisión

```{r c18-2, echo=FALSE}

Sys.Date()
```

```{r c18-3, echo=FALSE, fig.show = "hold", out.width = "20%", fig.align = "default"}
knitr::include_graphics(c("Graficos/hex_ggversa.png", "Graficos/hex_error.png"))
```



------------------------------------------------------------------------

Activar las librerías; asegúrate de tener instalados estos paquetes

```{r c18-4}
library(QuantPsyc)
library(car)
library(boot)
library(ggplot2)
library(tidyverse)
library(sjPlot)

```

------------------------------------------------------------------------


La regresión lineal es el modelo básico para evaluar si hay una relación *lineal* (o sea *recta*) entre dos variables. Esta relación entre las variables puede ser positiva o negativa. Hay otros tipos de regresión, que incluyen la regresión no lineal, tal como la cuadrática ($x^2$), la cúbica ($x^3$) o la logarítmica ($\log(x)$), entre muchas otras alternativas.

Aquí estaremos evaluando solamente la regresión lineal

El primer paso es subir los datos:

El número de barres (bares) en un barrio y la cantidad de asesinatos en ese barrio (datos ficticios).

```{r c18-5}
library(readr)
pubs <- read_csv("Data_files_csv/pubs.csv")
pubs


pubs2=tribble(~pubs, ~mortality,
             10, 1000,
             20, 200,
             30, 30,
             40, 4,
             50, 1)
pubs2

# Cambiamos los nombres de las columnas

pubs=pubs%>%
  dplyr::rename(barres=pubs, mortalidad=mortality)

```

------------------------------------------------------------------------

## La función lm()

::: {.definicion}
**Regresión lineal simple.** Modelo que describe la relación *lineal* entre una variable predictora ($x$) y una variable de respuesta ($y$) mediante una recta, $y=\beta_0+\beta_1 x$. A diferencia de la correlación (que solo mide la fuerza de la asociación), la regresión permite **predecir** el valor de $y$ a partir de $x$.
:::

Cómo hacer una regresión lineal (Linear Regression) simple, usando la función **lm()** (de "linear model" = modelo lineal). Una regresión necesita dos variables continuas (vea el módulo de correlación). Es importante que estas variables tengan una distribución normal. La diferencia entre una correlación y una regresión es que la primera es un análisis que describe el patrón, mientras que la segunda no solamente describe el patrón, sino que también hace predicciones sobre la relación entre las variables. Usando la regresión uno calcula también una línea que describe la relación entre las variables. Esta línea se puede describir como $y=mx+b$, donde la **m** representa la pendiente y la *b* representa el intercepto. También la pueden ver en los libros de la siguiente forma: $y=\alpha+\beta x$, donde $\beta$ (*beta*) representa la pendiente y $\alpha$ el intercepto.

La fórmula de lm() se compone de lm(y ~ x, data = "df"). Nota la tilde **~**. Hay dos pruebas; la primera para determinar si $\alpha$ (el intercepto) es distinto de cero. La hipótesis nula es:

Ho: el intercepto $\alpha$ es igual a cero.  
Ha: el intercepto $\alpha$ no es igual a cero. Entonces, el punto donde la línea corta el eje puede estar por encima o por debajo de cero.

La segunda hipótesis nula es que la pendiente es igual a cero; esto quiere decir que la pendiente no sugiere ningún patrón (de aumento o disminución) entre las dos variables.

Ho: la pendiente $\beta$ es igual a cero.  
Ha: la pendiente $\beta$ no es igual a cero. Entonces, la relación entre las dos variables es positiva o negativa.

Ahora evaluamos los resultados de la regresión entre el número de barres en un vecindario y la mortalidad en ese mismo sector. Se observa que los coeficientes de la línea son $y=3352+14.3\,x$. Entonces, el intercepto comienza en 3352 fatalidades y por cada bar adicional hay 14.3 fatalidades más.

Ahora, para determinar si estos valores son significativos, hay que evaluar el valor de **p** en cada línea. La hipótesis nula del intercepto tiene un valor de p = 0.005, lo que sugiere que se debe rechazar la hipótesis nula y, por consiguiente, aceptamos la hipótesis alterna. La pendiente tiene un valor de p = 0.015, y también se rechaza la hipótesis nula.

```{r c18-6, fig.align='center'}
pubReg <- lm(mortalidad~barres, data = pubs) 

model <- lm(mortality~pubs, data=pubs2)
summary(model)
#summary(pubReg) # Si no se acuerda de la funcíon que sigue

tab_model(
  pubReg,show.df = TRUE,
  CSS = list(
    css.depvarhead = 'color: red;',
    css.centeralign = 'text-align: left;', 
    css.firsttablecol = 'font-weight: bold;', 
    css.summary = 'color: blue;'
  )
)
```

------------------------------------------------------------------------

## Visualización de la regresión

Se observa que hay un aumento en las fatalidades con el aumento en el número de barres. Pero nota el valor a la derecha, que parece ser muy atípico comparado con los otros.

```{r c18-7}
ggplot(pubs, aes(x=barres, y=mortalidad))+
  geom_smooth(method = lm)+ # modelo lineal
  geom_point()

```




```{r c18-8}

datosP=tribble(~edad_casar, ~Num_hijos,
               NA, 10,
               22, 3,
               17, 2,
               18, 2,
               21, 5,
               20, 3,
               37, 2)
datosP

model2=lm(Num_hijos~edad_casar, data=datosP)
summary(model2)

ggplot(datosP, aes(y=Num_hijos, x=edad_casar))+
  geom_smooth(method = lm)+ # modelo lineal
  geom_point()

```

------------------------------------------------------------------------

## El impacto de valores atípicos

::: {.advertencia}
Un solo valor atípico o muy influyente puede cambiar drásticamente la pendiente, el intercepto y las conclusiones de una regresión. Por eso hay que visualizar siempre los datos y evaluar la influencia de cada punto (por ejemplo, con la distancia de Cook).
:::

En ciertas ocasiones, los valores fuera de lo normal pueden producir grandes cambios en el resultado, en este caso la regresión. ¿Cuál es el efecto del valor grande? Removemos ese valor del archivo de datos y reevaluamos el modelo (la regresión lineal). Nota que ahora el modelo es sumamente diferente: $y=-163.7+103.2\,x$. ¿Ahora se rechazan las dos hipótesis?

```{r c18-9}

pubsnew <- pubs[ which(pubs$barres<80), ]  # remover el valor grande

pubsnew=pubsnew %>% 
  add_row(barres = 4, mortalidad = 0) # Añadiendo un par de valores


pubRegNew <- lm(mortalidad~barres, data = pubsnew)
#summary(pubRegNew)

tab_model(
  pubRegNew,show.df = TRUE,
  CSS = list(
    css.depvarhead = 'color: red;',
    css.centeralign = 'text-align: left;', 
    css.firsttablecol = 'font-weight: bold;', 
    css.summary = 'color: blue;'
  )
)

ggplot(pubsnew,aes(x=barres, y=mortalidad))+
  geom_smooth(method=lm)+
  geom_point()


```

------------------------------------------------------------------------

## La venta de discos de música

Ahora un conjunto de datos más complejo y realista, parecido a los que uno encontraría en un estudio de medicina, sociología o ecología.

Los datos representan la cantidad de dinero dedicada a la promoción de diferentes CD de una compañía de música y la cantidad de CD (CD/descargas) vendidos. En la primera fila se observa la cantidad de libras esterlinas, **£** (UK), dedicada a la promoción del álbum de música: vemos que se gastaron £10,256 y que después la cantidad de CD vendidos fue 330.

```{r c18-10}
library(readr)
Album_Sales_1_new <- read_csv("Data_files_csv/Album_Sales_1_new.csv")
head(Album_Sales_1_new)


length(Album_Sales_1_new$adverts)  # cuántas filas de datos hay en el data frame
```

Comenzamos haciendo un gráfico entre las dos variables. Nota que en la parte de **geom_smooth()** tienes que incluir **method=lm**; esto significa que el método para construir la línea usará la regresión lineal. Se añade a la función lineal el término $\epsilon$, que representa los errores de los valores al compararlos con la línea que representa el mejor modelo.

$$Y_{ i }=\beta _{ 0 }+\beta _{ 1 }X_{ i }+\epsilon _{ i }$$

::: {.formula}
$Y_i$ = valor observado de la respuesta; $\beta_0$ = intercepto (valor de $Y$ cuando $X=0$); $\beta_1$ = pendiente (cambio en $Y$ por cada unidad de $X$); $X_i$ = valor de la variable predictora; $\epsilon_i$ = error o residual (la parte de $Y$ que el modelo no explica).
:::

Recuerda que $\beta _{ 0 }$ es el intercepto y $\beta _{ 1 }$ es la pendiente. El área sombreada es el intervalo de confianza de 95%. Esto quiere decir que la mejor línea (intercepto y pendiente) podría variar dentro de ese rango si repitiéramos el experimento. Para ilustrar las alternativas, añadí los dos extremos: una pendiente mayor (roja) y una menor (violeta). Cada punto representa la venta de CD con su correspondiente cantidad dedicada a la promoción. Los $\epsilon$ serían la diferencia entre la mejor línea y el valor observado; esto se llama también los residuales.

```{r c18-11}
library(ggplot2)
ggplot(Album_Sales_1_new,aes(x=adverts, y=sales))+
  geom_smooth(method=lm, se = TRUE)+
  geom_point()+
  geom_segment(aes(x=0, y=120, xend=2250, yend=380), colour="red")+
  geom_segment(aes(x=0, y=150, xend=2250, yend=320), colour="purple")
```

El modelo lineal con la función **lm()**. ¿Cómo interpretas los coeficientes? ¿Son significativos? ¿Se rechaza la hipótesis nula?

```{r c18-12}
library(sjPlot)
head(Album_Sales_1_new)


model1=lm(sales~adverts, Album_Sales_1_new)

#summary(model1)

tab_model(
  model1,show.df = TRUE,
  CSS = list(
    css.depvarhead = 'color: red;',
    css.centeralign = 'text-align: left;', 
    css.firsttablecol = 'font-weight: bold;', 
    css.summary = 'color: blue;'
  )
)
```

------------------------------------------------------------------------

### Supuestos de la regresión lineal

::: {.supuestos}
Los supuestos de la regresión lineal se evalúan con `plot(modelo)`:

1. **Igualdad de varianza (homocedasticidad):** en el primer gráfico, evaluar si los residuales están distribuidos más o menos por igual, sin más variación en un área del gráfico que en otra.
2. **Normalidad de los residuales:** en el segundo gráfico, evaluar el gráfico Q-Q.
3. **Valores atípicos:** evaluar si hay datos sesgados (atípicos) que influyan en los resultados.
4. **Observaciones influyentes:** en el cuarto gráfico, evaluar si hay valores que tienen mucho peso sobre el modelo según se incluyan o no en el análisis (distancia de Cook).
:::

En el presente gráfico hay tres valores que hay que evaluar (1, 42, 169); hay que asegurarse de que estos valores son correctos. Siempre es bueno reevaluar el análisis con y sin los valores sesgados para observar cuán diferentes son los resultados.

```{r c18-13}
plot(model1)  # Evaluar los supuestos, 1. Igualdad de varianza, 2. Normalidad, 4. Datos sesgados (Cook's Distance)
```

------------------------------------------------------------------------

## Cook's Distance

Evaluar si hay valores que sesgan los resultados.

Definición (basada en Wikipedia): los puntos con residuales grandes (atípicos) y/o alto apalancamiento (*leverage*) pueden distorsionar el resultado y la exactitud de una regresión. La distancia de Cook mide el efecto de eliminar una observación dada. Los puntos con una distancia de Cook grande merecen un examen más cuidadoso en el análisis.

**Cómo detectar observaciones muy influyentes:** hay diferentes opiniones sobre qué valores de corte usar. Una guía sencilla es $D_i > 1$. Otros sugieren usar $D_i > 4/n$, donde $n$ es el número de observaciones.

Aquí muestro cómo: 1. añadir los valores de "cook.distance" a tu archivo; 2. añadir una columna de "secuencia" de los datos; 3. crear una gráfica de las distancias de Cook.  
4. ¿Qué valores tienen $D_i$ mayor de 1? ¿Qué valores tienen distancias de Cook mayores de $4/n$?

```{r c18-14}
4/length(Album_Sales_1_new$adverts)



Album_Sales_1_new$cooks.distance<-cooks.distance(model1)
head(Album_Sales_1_new)

Album_Sales_1_new$sequence=c(1:200)
head(Album_Sales_1_new)
tail(Album_Sales_1_new)

ggplot(Album_Sales_1_new, aes(sequence, cooks.distance))+
  geom_point()+
  geom_hline(aes(yintercept=4/length(Album_Sales_1_new$adverts), colour="red"))
```

------------------------------------------------------------------------

## Repaso de los PASOS

## Paso 1

1. Primer paso, mirar los coeficientes.

  - El resultado: El coeficiente (intercepto) y la pendiente del *model1*

### Paso 2

2.  Evaluar si los coeficientes son diferentes de cero.

```{=html}
<!-- -->
```
a.  Primera hipótesis: determinar si el intercepto es igual a cero. Mira el valor de p, Pr(>|t|); si el valor es menor de 0.05, se rechaza la Ho y, por consiguiente, tenemos confianza de que el intercepto no incluye cero.

b.  Segunda hipótesis nula: determinar si la pendiente es igual a cero. Mira el valor de p, Pr(>|t|); como el valor es menor de 0.05, se rechaza la Ho y, por consiguiente, tenemos confianza de que la pendiente no incluye cero.

## Paso 3

3.  Evaluar si los datos cumplen con los supuestos.

4.  Igualdad de varianza: usa la gráfica de residuales.

5.  Normalidad: gráfico Q-Q.

6.  Valores sesgados: la distancia de Cook.

```{r c18-15}
advertizingReg <- lm(sales~adverts, data = Album_Sales_1_new)
summary(advertizingReg)

tab_model(
  advertizingReg,show.df = TRUE,
  CSS = list(
    css.depvarhead = 'color: red;',
    css.centeralign = 'text-align: left;', 
    css.firsttablecol = 'font-weight: bold;', 
    css.summary = 'color: blue;'
  )
)
```

------------------------------------------------------------------------

## Alternativa para Gráficar los residuales

### Graficar los residuales y los valores ajustados

#### Evaluación de los residuales

Si observamos que los residuales están distribuidos al azar (sin patrón, alrededor de una línea recta), entonces podemos asumir que hay igualdad de varianza a lo largo del eje "x".

#### Definición de residual

Un residual es la cantidad que queda después de restar o tener en cuenta otras cosas (basado en Wikipedia). Dicho de otra forma, un residual (o desviación del ajuste) es una estimación observable del error estadístico, que no es observable directamente.

```{r c18-16}
# "advertizingReg", nota que este no es un data frame pero un modelo
# The main figure
ggplot(Album_Sales_1_new,aes(x=adverts, y=sales))+
  geom_smooth(method=lm, se = TRUE)+
  geom_point()

# Graficando los residuales, metodo 1
ggplot(model1, aes(x=.fitted, y=.resid))+
  geom_point()+
  geom_smooth(method=lm)

```



## Por qué podemos usar la alternativa anterior

Cuando R corre un análisis, hace muchos cálculos y guarda parte de estos cálculos como listas de datos.

Para ver algunos de los valores calculados, uno escribe el nombre del modelo y le añade el **$**, y debería aparecer la lista de los valores calculados. Otra alternativa es usar la función ls() para ver el nombre de las variables.

```{r c18-17}
ls(model1)

# model1$residuals  # Por ejemplo para ver los residuales del modelo
```



***

## Ejercicio #1 

Usa el archivo enviado por MS Teams llamado "Alturas_Humanos".

 - En este archivo vemos tres columnas: Género, Altura de la persona (Altura_cm) y Peso de la persona (Peso_kg).

 - Evalua cada paso para determinar si hay una relación entre la altura y el peso de las personas.
 - Haz un gráfico de la relación entre la altura y el peso
 - Evalúa el modelo de regresión entre las dos variables
 - Evalúa los supuestos
 

```{r c18-18}
library(readr)
Alturas_Humanos <- read_csv("Data_files_csv/Alturas_Humanos.csv")

head(Alturas_Humanos)
```


```{r c18-19, eval=FALSE, include=FALSE}
model2=lm(Altura_cm~Peso_kg, data=Alturas_Humanos)
summary(model2)
```

```{r c18-20, eval=FALSE, include=FALSE}
plot(model2)
```


```{r c18-21, eval=FALSE, include=FALSE}
ggplot(Alturas_Humanos, aes(Peso_kg, Altura_cm, color=Genero))+
geom_point()+
geom_smooth(method=lm)+
theme(axis.title=element_text(size=20,face="bold"),
axis.text=element_text(size=20, face="bold"))+
labs(x="Altura en cm", y="Peso")
```



***


## Ejercicio #2 

## Datos sobre la orquidea **Dipodium**

Los datos se encuentran en el paquete **ggversa** bajo el nombre *dipodium*.

 - Evalúa todos los pasos para ver si la altura de la inflorescencia es un predictor de la cantidad de flores que tiene una planta.

```{r c18-22, eval=FALSE, include=FALSE}
library(ggversa)
library(ggplot2)
```


```{r c18-23, eval=FALSE, include=FALSE}
ggplot(dipodium, aes(Height_Inflo, Number_of_Flowers))+
geom_point()+
geom_smooth(method=lm)+
theme(axis.title=element_text(size=20,face="bold"),
axis.text=element_text(size=20, face="bold"))+
labs(x="Altura de la Inflorescencia",y="Cantidad de Flores")
```


```{r c18-24, eval=FALSE, include=FALSE}
modelD=lm(Number_of_Flowers~ Height_Inflo, data = dipodium)
summary(modelD)

```



```{r c18-25, eval=FALSE, include=FALSE}
plot(modelD)
```


```{r c18-26, eval=FALSE, include=FALSE}
ggplot(dipodium, aes(Height_Inflo, Number_of_Flowers))+
geom_point()+
geom_abline(intercept=-2.197 , slope = 0.451)+
theme(axis.title=element_text(size=20,face="bold"),
axis.text=element_text(size=20, face="bold"))+
labs(x="Altura de la Inflorescencia",
y="Cantidad de Flores")
```




